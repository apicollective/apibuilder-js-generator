{
  label: '<%= helpers.makeLabel(field.name) %>',
  name: '<%= prefix + field.name %>',
  component: ComponentTypes.SUB_FORM,
  fields: [<% field.type.fields.forEach(function(modelField) { %><%- include(helpers.getFieldType(modelField.type), {
    field: modelField,
    helpers: helpers,
    prefix: prefix + field.name + '.'
  }); %><%}); %>],
},