{
  label: '<%= helpers.makeLabel(field.name) %>',
  name: '<%= prefix + field.name %>',<% if(helpers.getDataType(field.type)) { %> 
  dataType: <%= helpers.getDataType(field.type) %>,<% } %>
  component: <%= helpers.getComponentType(field.type) %>,<% if(helpers.getType(field.type)) { %>
  type: '<%= helpers.getType(field.type) %>',<% } %><% if(field.isRequired) { %>
  validate: [
    { type: ValidatorTypes.REQUIRED },
  ],<% } %>
},